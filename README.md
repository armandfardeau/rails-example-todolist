# README

This application is used to teach the introductory course on MVC architecture.

Want to test? Sure!

# Install

* Install dependencies
`bundle install`

* Execute migrations
`rails db:migrate`

* Start local server
`rails server`